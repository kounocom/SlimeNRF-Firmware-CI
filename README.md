# SlimeNRF-Firmware-CI
SlimeNRF-Firmware-CI

For automatic Release of CI builds, go to Repository > Settings > Security > Secrets and variables > Actions, create a Repository Secret named "GH_TOKEN" with your Personal access token (Fine-grained or classic).
